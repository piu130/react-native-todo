const PREFIX = 'tmpTodo/'
export const NEW = `${PREFIX}NEW`
export const SET = `${PREFIX}SET`
export const REMOVE = `${PREFIX}REMOVE`
export const CHANGE_PROPERTY = `${PREFIX}CHANGE_PROPERTY`
export const CLEAR_DATETIME = `${PREFIX}CLEAR_DATETIME`
